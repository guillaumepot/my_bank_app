# API env file, contains critic vars for the API


# API configuration
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000

# API Auth configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRATION=30
CRYPT_CONTEXT_SCHEME=argon2
# API Security configuration | Allowed usernames for the API
AUTHORIZED_USERS =root,root2 # Do not put spaces between users, 
AUTH_LIMIT=5/hour

# Database connection
# Update user and password for production use
POSTGRES_HOST=bank_app_postgres
POSTGRES_PORT=5432
POSTGRES_USER=root
POSTGRES_PASSWORD=root
POSTGRES_DB=bank_db